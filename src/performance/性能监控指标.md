### 页面加载时间 page load time
const pageLoadTime = timing.loadEventEnd - timing.navigationStart

### 首次内容绘制时间 first contentful paint(FCP)
new Performance0bserver((entryList)=> {
  const entries = entrylist.getEntriesByName('first-contentful-paint');
  if(entries.length>0){
    const fcp =entries[0];
  }
}).observe({ type: 'paint', buffered: true });

### 最大内容绘制时间 largest contentful paint(LCP)
new Performance0bserver((entryList)=> {
  const entries = entrylist.getEntries()
  const lastEntry = entries[entries.length - 1]
}).observe({ type: 'largest-contentful-paint', buffered: true });

### 交互时间 time to interactive(TTI)
const tti = performance.timing.domInteractive - performance.timing.navigationStart

### 资源加载时间 resource timing
const resources = performance.getEntriesByType('resource');
resources.forEach((resource) => {
  <!-- 过滤极短时间的资源请求，关注耗时较长的资源 -->
  if (resource.responseEnd - resource.startTime > 50) {}
})